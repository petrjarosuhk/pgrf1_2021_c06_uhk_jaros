package clip;

import model.Polygon;

public class Clipper {
    /*private Polygon sourcePoly; //orezavany (input)
    private Polygon clipPoly; //orezavaci (clipper)
    private Polygon resultPoly; //orezany (output)
*/
    public static Polygon clip(Polygon sourcePoly, Polygon clipPoly){
        Math.sin(3.14);
        return null;
    }
}
